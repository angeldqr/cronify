{
  "buildCommand": "cd frontend && npm install && npx quasar build",
  "outputDirectory": "frontend/dist/spa",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}